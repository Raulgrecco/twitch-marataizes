<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Festival de Cinema de Marataízes</title>
  <meta name="description" content="Festival de Cinema de Marataízes — Mostra de curtas, formação audiovisual e cultura capixaba. Próxima edição: 2026." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --ink:     #0d0d0d;
      --paper:   #f5f0e8;
      --white:   #ffffff;
      --gold:    #c8935a;
      --gold-lt: #e3b882;
      --muted:   #7a7265;
      --rule:    rgba(13,13,13,.12);

      --serif:   'Playfair Display', Georgia, serif;
      --sans:    'DM Sans', sans-serif;

      --space-xs: .5rem;
      --space-sm: 1rem;
      --space-md: 2rem;
      --space-lg: 4rem;
      --space-xl: 7rem;

      --max: 1100px;
      --transition: .35s cubic-bezier(.4,0,.2,1);
    }

    html { scroll-behavior: smooth; font-size: 16px; }

    body {
      background: var(--paper);
      color: var(--ink);
      font-family: var(--sans);
      font-weight: 300;
      line-height: 1.7;
      -webkit-font-smoothing: antialiased;
    }

    img { display: block; max-width: 100%; }
    a { color: inherit; text-decoration: none; }

    .container {
      width: 90%;
      max-width: var(--max);
      margin-inline: auto;
    }

    .section-label {
      font-family: var(--sans);
      font-weight: 500;
      font-size: .7rem;
      letter-spacing: .22em;
      text-transform: uppercase;
      color: var(--gold);
      display: block;
      margin-bottom: var(--space-sm);
    }

    .section-title {
      font-family: var(--serif);
      font-size: clamp(1.8rem, 4vw, 2.8rem);
      font-weight: 700;
      line-height: 1.2;
      margin-bottom: var(--space-md);
    }

    .divider {
      width: 48px;
      height: 2px;
      background: var(--gold);
      margin-bottom: var(--space-md);
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      padding: 1.2rem 0;
      transition: background var(--transition), box-shadow var(--transition);
    }

    nav.scrolled {
      background: rgba(245,240,232,.96);
      box-shadow: 0 1px 0 var(--rule);
      backdrop-filter: blur(8px);
    }

    .nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav-logo {
      font-family: var(--serif);
      font-size: .95rem;
      font-weight: 700;
      letter-spacing: .02em;
      line-height: 1.2;
    }

    .nav-logo span { color: var(--gold); }

    .nav-links {
      display: flex;
      gap: var(--space-md);
      list-style: none;
    }

    .nav-links a {
      font-size: .78rem;
      font-weight: 500;
      letter-spacing: .1em;
      text-transform: uppercase;
      opacity: .7;
      transition: opacity var(--transition), color var(--transition);
    }

    .nav-links a:hover { opacity: 1; color: var(--gold); }

    @media (max-width: 640px) { .nav-links { display: none; } }

    /* HERO */
    #hero {
      min-height: 100svh;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding-bottom: var(--space-xl);
      position: relative;
      overflow: hidden;
      background: var(--ink);
      color: var(--white);
    }

    #hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.06'/%3E%3C/svg%3E");
      opacity: .35;
      pointer-events: none;
    }

    #hero::after {
      content: '';
      position: absolute;
      top: -10%;
      right: -5%;
      width: clamp(280px, 45vw, 600px);
      aspect-ratio: 1;
      border: 1px solid rgba(200,147,90,.18);
      border-radius: 50%;
      pointer-events: none;
    }

    .hero-eyebrow {
      font-size: .72rem;
      letter-spacing: .28em;
      text-transform: uppercase;
      color: var(--gold-lt);
      font-weight: 500;
      margin-bottom: 1.5rem;
      opacity: 0;
      animation: fadeUp .8s .2s forwards;
    }

    .hero-title {
      font-family: var(--serif);
      font-size: clamp(2.6rem, 8vw, 6.5rem);
      font-weight: 900;
      line-height: 1.0;
      letter-spacing: -.01em;
      margin-bottom: 1.5rem;
      opacity: 0;
      animation: fadeUp .9s .35s forwards;
    }

    .hero-title em {
      font-style: italic;
      color: var(--gold);
    }

    .hero-sub {
      font-size: clamp(.85rem, 1.8vw, 1.05rem);
      color: rgba(255,255,255,.65);
      letter-spacing: .05em;
      margin-bottom: var(--space-md);
      opacity: 0;
      animation: fadeUp .9s .5s forwards;
    }

    .hero-sub span { color: rgba(255,255,255,.35); margin-inline: .5rem; }

    .hero-cta {
      display: inline-block;
      border: 1px solid rgba(200,147,90,.5);
      color: var(--gold-lt);
      padding: .85rem 2rem;
      font-size: .78rem;
      font-weight: 500;
      letter-spacing: .15em;
      text-transform: uppercase;
      transition: background var(--transition), border-color var(--transition), color var(--transition);
      opacity: 0;
      animation: fadeUp .9s .65s forwards;
      cursor: default;
    }

    .hero-cta:hover {
      background: rgba(200,147,90,.1);
      border-color: var(--gold);
    }

    .hero-badge {
      position: absolute;
      top: 50%;
      right: 8%;
      transform: translateY(-50%);
      width: clamp(120px, 14vw, 180px);
      aspect-ratio: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      border: 1px solid rgba(200,147,90,.4);
      border-radius: 50%;
      padding: 1.5rem;
      opacity: 0;
      animation: fadeIn 1s 1s forwards;
    }

    .hero-badge-text {
      font-family: var(--serif);
      font-size: clamp(.7rem, 1.2vw, .9rem);
      line-height: 1.4;
      color: rgba(255,255,255,.75);
    }

    .hero-badge-year {
      font-size: clamp(1.4rem, 3vw, 2rem);
      font-weight: 900;
      color: var(--gold);
      display: block;
      font-family: var(--serif);
    }

    @media (max-width: 640px) { .hero-badge { display: none; } }

    /* SECTIONS */
    section { padding: var(--space-xl) 0; }
    section:nth-child(even) { background: var(--white); }

    /* SOBRE */
    #sobre .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-xl);
      align-items: start;
    }

    @media (max-width: 768px) {
      #sobre .grid { grid-template-columns: 1fr; gap: var(--space-lg); }
    }

    #sobre .text p {
      font-size: 1.05rem;
      line-height: 1.85;
      color: #3a3530;
      max-width: 52ch;
    }

    .stat-block {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-sm);
      margin-top: var(--space-md);
    }

    .stat {
      border-top: 1px solid var(--rule);
      padding-top: var(--space-sm);
    }

    .stat-num {
      font-family: var(--serif);
      font-size: 2.2rem;
      font-weight: 700;
      color: var(--gold);
      line-height: 1;
    }

    .stat-label {
      font-size: .78rem;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: var(--muted);
      margin-top: .25rem;
    }

    .logo-visual {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-lg);
      border: 1px solid var(--rule);
    }

    /* PROGRAMAÇÃO */
    #programacao {
      background: var(--ink);
      color: var(--white);
    }

    #programacao .section-title { color: var(--white); }
    #programacao .section-label { color: var(--gold-lt); }

    .prog-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1px;
      background: rgba(255,255,255,.07);
      border: 1px solid rgba(255,255,255,.07);
      margin-top: var(--space-md);
    }

    .prog-item {
      padding: var(--space-md) 1.5rem;
      background: var(--ink);
      transition: background var(--transition);
    }

    .prog-item:hover { background: #161410; }

    .prog-icon {
      font-size: 1.5rem;
      margin-bottom: var(--space-sm);
    }

    .prog-item h3 {
      font-family: var(--serif);
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: .5rem;
      color: var(--paper);
    }

    .prog-item p {
      font-size: .85rem;
      color: rgba(255,255,255,.45);
      line-height: 1.65;
    }

    .prog-tag {
      display: inline-block;
      margin-top: var(--space-sm);
      font-size: .65rem;
      letter-spacing: .15em;
      text-transform: uppercase;
      color: var(--gold);
      border: 1px solid rgba(200,147,90,.3);
      padding: .2rem .6rem;
    }

    /* FORMAÇÃO */
    #formacao .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--space-sm);
      margin-top: var(--space-md);
    }

    .card {
      background: var(--paper);
      border: 1px solid var(--rule);
      padding: var(--space-md) 1.5rem;
      transition: border-color var(--transition), transform var(--transition);
    }

    .card:hover {
      border-color: var(--gold);
      transform: translateY(-3px);
    }

    .card-num {
      font-family: var(--serif);
      font-size: 2.5rem;
      font-weight: 900;
      color: rgba(200,147,90,.2);
      line-height: 1;
      margin-bottom: .5rem;
    }

    .card h3 {
      font-family: var(--serif);
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: .4rem;
    }

    .card p { font-size: .82rem; color: var(--muted); line-height: 1.6; }

    /* ACESSIBILIDADE */
    #acessibilidade .inner {
      display: grid;
      grid-template-columns: 1fr 2fr;
      gap: var(--space-xl);
      align-items: center;
    }

    @media (max-width: 768px) {
      #acessibilidade .inner { grid-template-columns: 1fr; gap: var(--space-md); }
    }

    #acessibilidade .big-a {
      font-family: var(--serif);
      font-size: clamp(6rem, 18vw, 12rem);
      font-weight: 900;
      line-height: 1;
      color: transparent;
      -webkit-text-stroke: 1px var(--gold);
      opacity: .3;
      user-select: none;
    }

    #acessibilidade p {
      font-size: 1rem;
      line-height: 1.85;
      color: #3a3530;
      margin-bottom: var(--space-sm);
    }

    .badge-row {
      display: flex;
      flex-wrap: wrap;
      gap: .5rem;
      margin-top: var(--space-sm);
    }

    .badge {
      font-size: .7rem;
      letter-spacing: .1em;
      text-transform: uppercase;
      font-weight: 500;
      padding: .35rem .9rem;
      border: 1px solid var(--gold);
      color: var(--gold);
    }

    /* DIFUSÃO */
    #difusao {
      background: var(--ink);
      color: var(--white);
    }

    #difusao .section-title { color: var(--white); }
    #difusao .section-label { color: var(--gold-lt); }

    #difusao .inner {
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: var(--space-xl);
      align-items: center;
    }

    @media (max-width: 768px) {
      #difusao .inner { grid-template-columns: 1fr; }
    }

    #difusao p {
      color: rgba(255,255,255,.65);
      font-size: 1rem;
      line-height: 1.9;
      margin-bottom: var(--space-sm);
    }

    .channel-block {
      border: 1px solid rgba(255,255,255,.08);
      padding: var(--space-md);
    }

    .channel-block h3 {
      font-family: var(--serif);
      font-size: 1.1rem;
      margin-bottom: .75rem;
      color: var(--paper);
    }

    .channel-link {
      display: flex;
      align-items: center;
      gap: .75rem;
      padding: .9rem 0;
      border-bottom: 1px solid rgba(255,255,255,.06);
      color: rgba(255,255,255,.65);
      font-size: .88rem;
      transition: color var(--transition);
    }

    .channel-link:last-child { border-bottom: none; }
    .channel-link:hover { color: var(--gold-lt); }

    .channel-link .icon {
      width: 32px;
      height: 32px;
      border: 1px solid rgba(200,147,90,.3);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: .9rem;
      flex-shrink: 0;
    }

    /* CONTATO */
    #contato {
      text-align: center;
      padding: var(--space-xl) 0;
    }

    #contato .section-title { margin-bottom: var(--space-sm); }

    #contato p {
      color: var(--muted);
      font-size: .95rem;
      max-width: 50ch;
      margin-inline: auto;
      margin-bottom: var(--space-lg);
    }

    .contact-links {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-sm);
      justify-content: center;
    }

    .contact-btn {
      display: inline-flex;
      align-items: center;
      gap: .6rem;
      padding: 1rem 2rem;
      border: 1px solid var(--ink);
      font-size: .82rem;
      font-weight: 500;
      letter-spacing: .12em;
      text-transform: uppercase;
      transition: background var(--transition), color var(--transition);
    }

    .contact-btn:hover { background: var(--ink); color: var(--paper); }

    .contact-btn.primary {
      background: var(--ink);
      color: var(--paper);
    }

    .contact-btn.primary:hover {
      background: var(--gold);
      border-color: var(--gold);
      color: var(--white);
    }

    /* FOOTER */
    footer {
      background: var(--ink);
      color: rgba(255,255,255,.35);
      padding: var(--space-md) 0;
      font-size: .75rem;
      letter-spacing: .05em;
    }

    .footer-inner {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-sm);
      justify-content: space-between;
      align-items: center;
    }

    .footer-brand {
      font-family: var(--serif);
      color: rgba(255,255,255,.7);
      font-size: .85rem;
    }

    /* ANIMATIONS */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to   { opacity: 1; }
    }

    .reveal {
      opacity: 0;
      transform: translateY(32px);
      transition: opacity .7s ease, transform .7s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .reveal-delay-1 { transition-delay: .1s; }
    .reveal-delay-2 { transition-delay: .2s; }
    .reveal-delay-3 { transition-delay: .3s; }
    .reveal-delay-4 { transition-delay: .4s; }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav id="nav">
    <div class="container nav-inner">
      <div class="nav-logo">Festival de Cinema<br><span>de Marataízes</span></div>
      <ul class="nav-links">
        <li><a href="#sobre">Sobre</a></li>
        <li><a href="#programacao">Programação</a></li>
        <li><a href="#formacao">Formação</a></li>
        <li><a href="#difusao">Difusão</a></li>
        <li><a href="#contato">Contato</a></li>
      </ul>
    </div>
  </nav>

  <!-- HERO -->
  <section id="hero">
    <div class="container">
      <p class="hero-eyebrow">Espírito Santo &mdash; Sul Capixaba &mdash; Audiovisual Independente</p>
      <h1 class="hero-title">Festival de<br>Cinema de<br><em>Marataízes</em></h1>
      <p class="hero-sub">
        Mostra de curtas
        <span>•</span>
        Formação audiovisual
        <span>•</span>
        Cultura capixaba
      </p>
      <a class="hero-cta" tabindex="0" aria-disabled="true">Chamada de filmes &mdash; em breve</a>
    </div>

    <div class="hero-badge">
      <div class="hero-badge-text">
        Próxima edição<br>
        <span class="hero-badge-year">2026</span>
      </div>
    </div>
  </section>

  <!-- SOBRE -->
  <section id="sobre">
    <div class="container">
      <div class="grid">
        <div class="text reveal">
          <span class="section-label">Sobre o Festival</span>
          <h2 class="section-title">Cinema público,<br>gratuito e capixaba</h2>
          <div class="divider"></div>
          <p>
            O Festival de Cinema de Marataízes é um evento público e gratuito dedicado à exibição de curtas-metragens capixabas e nacionais, com foco na formação de público, na valorização do audiovisual independente e no fortalecimento da cena cultural do sul do Espírito Santo.
          </p>
          <div class="stat-block">
            <div class="stat reveal reveal-delay-1">
              <div class="stat-num">ES</div>
              <div class="stat-label">Origem capixaba</div>
            </div>
            <div class="stat reveal reveal-delay-2">
              <div class="stat-num">Sul</div>
              <div class="stat-label">Espírito Santo</div>
            </div>
            <div class="stat reveal reveal-delay-3">
              <div class="stat-num">100%</div>
              <div class="stat-label">Gratuito e público</div>
            </div>
            <div class="stat reveal reveal-delay-4">
              <div class="stat-num">2026</div>
              <div class="stat-label">Próxima edição</div>
            </div>
          </div>
        </div>

        <!-- Logo SVG -->
        <div class="logo-visual reveal reveal-delay-2">
          <svg viewBox="0 0 280 280" xmlns="http://www.w3.org/2000/svg" width="240" height="240"
               role="img" aria-label="Logo do Festival de Cinema de Marataízes">
            <polygon points="140,18 268,252 12,252" fill="none" stroke="#0d0d0d" stroke-width="2"/>
            <polygon points="140,60 228,232 52,232" fill="none" stroke="#c8935a" stroke-width="1"/>
            <rect x="115" y="95" width="50" height="36" rx="3" fill="#0d0d0d"/>
            <circle cx="140" cy="113" r="11" fill="none" stroke="#0d0d0d" stroke-width="3"/>
            <circle cx="140" cy="113" r="6" fill="#c8935a"/>
            <rect x="125" y="89" width="14" height="7" rx="2" fill="#0d0d0d"/>
            <line x1="140" y1="131" x2="140" y2="160" stroke="#0d0d0d" stroke-width="1.5"/>
            <line x1="140" y1="150" x2="122" y2="175" stroke="#0d0d0d" stroke-width="1.2"/>
            <line x1="140" y1="150" x2="158" y2="175" stroke="#0d0d0d" stroke-width="1.2"/>
            <line x1="122" y1="165" x2="112" y2="185" stroke="#0d0d0d" stroke-width="1"/>
            <line x1="158" y1="165" x2="168" y2="185" stroke="#0d0d0d" stroke-width="1"/>
            <line x1="140" y1="160" x2="140" y2="185" stroke="#0d0d0d" stroke-width="1"/>
            <rect x="48" y="148" width="72" height="8" rx="1" fill="#0d0d0d" transform="rotate(-5,84,152)"/>
            <rect x="50" y="160" width="65" height="5" rx="1" fill="#c8935a" transform="rotate(-5,83,163)"/>
            <path d="M 180 195 Q 210 180 240 195" fill="none" stroke="#0d0d0d" stroke-width="2"/>
            <path d="M 180 205 Q 210 190 240 205" fill="none" stroke="#0d0d0d" stroke-width="1.5"/>
            <path d="M 180 215 Q 210 200 240 215" fill="none" stroke="#c8935a" stroke-width="1.5"/>
            <path d="M 110 235 A 30 18 0 0 1 170 235" fill="none" stroke="#0d0d0d" stroke-width="2"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- PROGRAMAÇÃO -->
  <section id="programacao">
    <div class="container">
      <span class="section-label reveal">Programação</span>
      <h2 class="section-title reveal">Em construção</h2>
      <p class="reveal" style="color:rgba(255,255,255,.5); max-width:52ch; margin-bottom:var(--space-md);">
        A programação da próxima edição está sendo preparada. Acompanhe nosso Instagram para novidades.
      </p>
      <div class="prog-grid">
        <div class="prog-item reveal reveal-delay-1">
          <div class="prog-icon">🎬</div>
          <h3>Mostra Competitiva</h3>
          <p>Exibição de curtas-metragens capixabas e nacionais selecionados para a competição oficial.</p>
          <span class="prog-tag">Em breve</span>
        </div>
        <div class="prog-item reveal reveal-delay-2">
          <div class="prog-icon">✨</div>
          <h3>Sessões Especiais</h3>
          <p>Mostras temáticas e exibições especiais fora da competição principal.</p>
          <span class="prog-tag">Em breve</span>
        </div>
        <div class="prog-item reveal reveal-delay-3">
          <div class="prog-icon">🎙</div>
          <h3>Rodas de Conversa</h3>
          <p>Encontros com realizadores, debatedores e profissionais do audiovisual capixaba e nacional.</p>
          <span class="prog-tag">Em breve</span>
        </div>
        <div class="prog-item reveal reveal-delay-4">
          <div class="prog-icon">🏆</div>
          <h3>Troféu Pérola Capixaba</h3>
          <p>Cerimônia de premiação com entrega do Troféu Pérola Capixaba aos destaques da mostra.</p>
          <span class="prog-tag">Premiação oficial</span>
        </div>
      </div>
    </div>
  </section>

  <!-- FORMAÇÃO -->
  <section id="formacao">
    <div class="container">
      <span class="section-label reveal">Ações Formativas</span>
      <h2 class="section-title reveal">Oficinas de<br>audiovisual</h2>
      <div class="divider reveal"></div>
      <p class="reveal" style="max-width:58ch; margin-bottom:var(--space-md); color:#3a3530; font-size:1rem; line-height:1.85;">
        Oficinas gratuitas de introdução ao audiovisual voltadas a jovens, estudantes e interessados em cinema da região. Um espaço de descoberta e formação para novos realizadores do sul do Espírito Santo.
      </p>
      <div class="cards">
        <div class="card reveal reveal-delay-1">
          <div class="card-num">01</div>
          <h3>Direção</h3>
          <p>Fundamentos da linguagem cinematográfica e direção de cena.</p>
        </div>
        <div class="card reveal reveal-delay-2">
          <div class="card-num">02</div>
          <h3>Roteiro</h3>
          <p>Narrativa audiovisual, estrutura dramática e escrita de roteiros curtos.</p>
        </div>
        <div class="card reveal reveal-delay-3">
          <div class="card-num">03</div>
          <h3>Edição</h3>
          <p>Montagem e pós-produção básica aplicada ao curta-metragem.</p>
        </div>
        <div class="card reveal reveal-delay-4">
          <div class="card-num">04</div>
          <h3>Produção</h3>
          <p>Gestão de projetos audiovisuais, captação de recursos e produção executiva.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ACESSIBILIDADE -->
  <section id="acessibilidade">
    <div class="container">
      <div class="inner">
        <div class="big-a reveal" aria-hidden="true">A</div>
        <div class="reveal reveal-delay-1">
          <span class="section-label">Acessibilidade</span>
          <h2 class="section-title">Cinema para<br>todos</h2>
          <div class="divider"></div>
          <p>O festival prevê ações de acessibilidade como interpretação em Libras em atividades institucionais e legendagem descritiva quando disponível, garantindo acesso pleno à programação.</p>
          <p>Nossa estrutura de acolhimento ao público é projetada para ser inclusiva e acessível a todos os participantes.</p>
          <div class="badge-row">
            <span class="badge">Libras</span>
            <span class="badge">Legendagem descritiva</span>
            <span class="badge">Estrutura acessível</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- DIFUSÃO -->
  <section id="difusao">
    <div class="container">
      <div class="inner">
        <div class="reveal">
          <span class="section-label">Difusão</span>
          <h2 class="section-title">Além das<br>telas presenciais</h2>
          <div class="divider" style="background:var(--gold-lt);"></div>
          <p>O festival contará com transmissão de partes da programação pela TV Marataízes, ampliando o alcance do audiovisual independente para além do espaço físico do evento.</p>
          <p>Conteúdos selecionados poderão ser exibidos posteriormente mediante autorização dos realizadores, em respeito aos direitos de cada obra. A divulgação contínua acontece pelo Instagram oficial do festival.</p>
        </div>
        <div class="channel-block reveal reveal-delay-2">
          <h3>Canais oficiais</h3>
          <a href="https://tvmarataizes.digital" target="_blank" rel="noopener" class="channel-link">
            <span class="icon">📺</span>
            TV Marataízes Digital
          </a>
          <a href="https://instagram.com/festivaldemarataizes" target="_blank" rel="noopener" class="channel-link">
            <span class="icon">📸</span>
            @festivaldemarataizes
          </a>
          <a href="https://macucaproducoes.com.br" target="_blank" rel="noopener" class="channel-link">
            <span class="icon">🌐</span>
            macucaproducoes.com.br
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- CONTATO -->
  <section id="contato">
    <div class="container">
      <span class="section-label reveal">Contato</span>
      <h2 class="section-title reveal">Fale com<br>o festival</h2>
      <p class="reveal">Acompanhe as novidades, chamadas e programação pelos nossos canais oficiais.</p>
      <div class="contact-links reveal">
        <a href="https://instagram.com/festivaldemarataizes" target="_blank" rel="noopener" class="contact-btn primary">
          Instagram
        </a>
        <a href="https://macucaproducoes.com.br" target="_blank" rel="noopener" class="contact-btn">
          macucaproducoes.com.br
        </a>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="container footer-inner">
      <span class="footer-brand">Festival de Cinema de Marataízes</span>
      <span>Realização: Macuca Produções &mdash; Sul do Espírito Santo</span>
      <span>tvmarataizes.digital/festivaldecinemademarataizes</span>
    </div>
  </footer>

  <script>
    // Nav scroll effect
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 40);
    }, { passive: true });

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const io = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          io.unobserve(e.target);
        }
      });
    }, { threshold: 0.12 });

    reveals.forEach(el => io.observe(el));
  </script>
</body>
</html>
